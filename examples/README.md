# examples

Real, anonymized output from running this skill. A repo without a worked example
reads as abandoned — so this folder holds at least one before/after.

**Rule:** anonymize the client, keep the numbers real. Never commit a raw
client export (see `.gitignore`).
